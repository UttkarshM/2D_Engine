{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/engine",
        "args": [],
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal"
      }
    ]
  }